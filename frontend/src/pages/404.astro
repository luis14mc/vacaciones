---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Página no encontrada" showNavbar={true}>
  <div class="min-h-screen bg-gray-50 flex flex-col justify-center items-center px-4">
    
    <div class="text-center">
      <!-- Logo o ilustración 404 -->
      <div class="mb-8">
        <svg class="mx-auto h-32 w-32 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M9.172 16.172a4 4 0 015.656 0M9 12h6m-6-4h6m6 12V6a2 2 0 00-2-2H5a2 2 0 00-2 2v14h14z"></path>
        </svg>
      </div>

      <!-- Título principal -->
      <h1 class="text-6xl font-bold text-gray-900 mb-4">404</h1>
      
      <!-- Subtitle -->
      <h2 class="text-2xl font-semibold text-gray-700 mb-4">Página no encontrada</h2>
      
      <!-- Descripción -->
      <p class="text-lg text-gray-600 mb-8 max-w-md mx-auto">
        Lo sentimos, no pudimos encontrar la página que buscas. 
        Puede que haya sido movida, eliminada o que hayas ingresado una URL incorrecta.
      </p>

      <!-- Botones de acción -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a 
          href="/" 
          class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-medium transition-colors inline-flex items-center"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          Ir al Inicio
        </a>
        
        <a 
          href="/dashboard" 
          class="bg-gray-600 hover:bg-gray-700 text-white px-6 py-3 rounded-lg font-medium transition-colors inline-flex items-center"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
          Dashboard
        </a>
        
        <button 
          onclick="history.back()" 
          class="text-blue-600 hover:text-blue-800 px-6 py-3 font-medium transition-colors inline-flex items-center"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          Regresar
        </button>
      </div>

      <!-- Enlaces útiles -->
      <div class="mt-12 pt-8 border-t border-gray-200">
        <p class="text-sm text-gray-600 mb-4">Enlaces útiles:</p>
        <div class="flex flex-wrap justify-center gap-4 text-sm">
          <a href="/dashboard" class="text-blue-600 hover:text-blue-800 transition-colors">Dashboard</a>
          <a href="/vacaciones" class="text-blue-600 hover:text-blue-800 transition-colors">Mis Vacaciones</a>
          <a href="/calendario" class="text-blue-600 hover:text-blue-800 transition-colors">Calendario</a>
          <a href="/perfil" class="text-blue-600 hover:text-blue-800 transition-colors">Mi Perfil</a>
        </div>
      </div>

    </div>

    <!-- Información de contacto opcional -->
    <div class="mt-16 text-center text-sm text-gray-500">
      <p>¿Necesitas ayuda? Contacta al administrador del sistema</p>
      <p class="mt-1">CNI Honduras - Sistema de Gestión de Vacaciones</p>
    </div>

  </div>
</Layout>

<style>
  /* Animación suave para los elementos */
  .text-center > * {
    animation: fadeInUp 0.6s ease-out;
  }
  
  .text-center > *:nth-child(2) {
    animation-delay: 0.1s;
  }
  
  .text-center > *:nth-child(3) {
    animation-delay: 0.2s;
  }
  
  .text-center > *:nth-child(4) {
    animation-delay: 0.3s;
  }
  
  .text-center > *:nth-child(5) {
    animation-delay: 0.4s;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Hover effects para los botones */
  a, button {
    transform: translateY(0);
    transition: all 0.2s ease-in-out;
  }
  
  a:hover, button:hover {
    transform: translateY(-1px);
  }
</style>